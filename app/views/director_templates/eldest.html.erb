<h1>Eldest director</h1>

<div>
  The eldest director in our dataset right now is <a href="/directors/<%= @director.id %>"><%= @director.name %></a>, born on <%= @director.dob.strftime("%B %e, %Y") %>.
</div>

<p>
  (<a href="http://strftime.net/">http://strftime.net/</a> is handy for putting together the formatting strings for the .strftime method.)
</p>
